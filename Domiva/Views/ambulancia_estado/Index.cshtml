@model IEnumerable<Domiva.Models.ambulancia_estado>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ambulancia.patente)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.estado_ambulancia.Descripsion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Ambulancia.patente)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.estado_ambulancia.Descripsion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_estadoambulancia }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_estadoambulancia }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_estadoambulancia })
        </td>
    </tr>
}

</table>
