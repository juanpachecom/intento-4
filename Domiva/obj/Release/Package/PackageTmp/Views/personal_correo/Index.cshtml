@model IEnumerable<Domiva.Models.personal_correo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AspNetUsers.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Personal.Rut_personal)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AspNetUsers.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Personal.Rut_personal)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_p_co }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_p_co }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_p_co })
        </td>
    </tr>
}

</table>
