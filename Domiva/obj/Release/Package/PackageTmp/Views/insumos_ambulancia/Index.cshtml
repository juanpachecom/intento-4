@model IEnumerable<Domiva.Models.insumos_ambulancia>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.fecha)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ambulancia.patente)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.insumos.codigo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.fecha)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ambulancia.patente)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.insumos.codigo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_insumo_ambulancia }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_insumo_ambulancia }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_insumo_ambulancia })
        </td>
    </tr>
}

</table>
