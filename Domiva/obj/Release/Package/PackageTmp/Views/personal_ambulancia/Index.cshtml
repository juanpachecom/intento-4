@model IEnumerable<Domiva.Models.personal_ambulancia>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ambulancia.patente)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Personal.Rut_personal)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Ambulancia.patente)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Personal.Rut_personal)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_p_a }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_p_a }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_p_a })
        </td>
    </tr>
}

</table>
